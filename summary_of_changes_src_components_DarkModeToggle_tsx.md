# Summary of Changes: `src/components/DarkModeToggle.tsx`

*   **Updated Imports**: Updated `useTheme` import to pull from `../context/ThemeContextCore` instead of `../context/ThemeContext`. This aligns with the refactored context structure designed to resolve React Fast Refresh warnings.